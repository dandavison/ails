{
  "name": "ails-server",
  "version": "0.1.0",
  "description": "Language server that receives diagnostics via file watching",
  "main": "out/server.js",
  "engines": {
    "node": ">=14.0.0"
  },
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -watch -p ."
  },
  "dependencies": {
    "vscode-languageserver": "^9.0.0",
    "vscode-languageserver-textdocument": "^1.0.11"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
